# docker-compose-sample
Spawn multiple Docker containers that will run with SSH servers

```
docker build -t sshd .
docker-compose up -d
docker-compose stop
docker-compose rm
docker-compose down 
```
